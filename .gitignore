node_modules
.next
out
build
dist

.env
.env.local
.env*.local

.DS_Store
*.log
.vercel
.turbo

next-env.d.ts
tsconfig.tsbuildinfo

# 콘텐츠 기획 참고자료 (이미지 포함 대용량, 로컬 전용)
caselab_contents creator/

# Python 캐시
__pycache__/
*.pyc

# 엔딩 카드 재생성 산출물 (원본은 Supabase cardpress/endings)
.cardpress-endings/

# 콘텐츠 초안 원고 (정본은 DB — admin에 초안으로 적재해서 검수·발행한다)
# components/content/는 코드라 지우면 안 되므로 루트만 지정(/content/).
/content/
